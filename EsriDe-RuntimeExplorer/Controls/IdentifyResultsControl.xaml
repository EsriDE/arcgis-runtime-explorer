<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:EsriDe.RuntimeExplorer.Controls"
             xmlns:viewModel="clr-namespace:EsriDe.RuntimeExplorer.ViewModel" x:Class="EsriDe.RuntimeExplorer.Controls.IdentifyResultsControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:IdentifyResultsViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding LayerLeftCommand}"/>
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="{Binding SelectedLayerDisplayIndex}"/>
                <Label Grid.Column="1" Content=" / "/>
                <Label Grid.Column="2" Content="{Binding LayerCount}"/>
            </Grid>
            <Button Grid.Column="2" Command="{Binding LayerRightCommand}"/>
            <Button Grid.Column="3" Command="{Binding ElementLeftCommand}"/>
            <Grid Grid.Column="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="{Binding SelectedElementDisplayIndex}"/>
                <Label Grid.Column="1" Content=" / "/>
                <Label Grid.Column="2" Content="{Binding ElementCount}"/>
            </Grid>
            <Button Grid.Column="5" Command="{Binding ElementRightCommand}"/>
        </Grid>
        <Grid Grid.Row="1">
            <xctk:PropertyGrid SelectedObject="{Binding SelectedGeoElement}" />
        </Grid>
    </Grid>
</UserControl>
